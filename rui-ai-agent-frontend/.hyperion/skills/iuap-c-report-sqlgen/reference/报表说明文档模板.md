# {报表名称} — 报表说明

> **SQL 脚本文件**：与同目录下的 **`{报表名称}.sql`** 配套使用；**请直接打开 `.sql` 文件复制到数据库客户端执行**。本文件仅作说明，不重复粘贴完整 SQL，避免双处维护。

## 一、报表说明

{简述业务场景、统计口径、适用对象。}

## 二、表关联关系

{可用 ASCII/缩进图或文字描述主表、子表、参照表及 JOIN 条件。}

## 三、SQL 文件说明

| 文件 | 说明 |
|------|------|
| `{报表名称}.sql` | 标准报表 SQL（或语义脚本 SQL），**仅此文件为可执行脚本** |
| `{报表名称}_特征表校验.sql` | （可选）当涉及 `character_define` 特征字段时，用于校验真实表名列名的诊断 SQL |

若未生成特征表校验文件，可忽略上表第二行。

## 四、字段映射说明

| 报表字段 | 来源表 | 来源字段 | 说明 |
|---------|--------|----------|------|
| … | … | … | … |

## 五、注意事项

1. …
2. …

## 六、元数据参考（可选）

{摘录相关 entities 的 schema、tableName、uri 等，便于核对。}
